<!-- 指定模型名称，标识为 g1 scene -->
<mujoco model="g1 scene">
<!-- 包含外部 XML 文件，扩展模型定义 -->
<!-- 将另一个 g1_29dof_rev_1_0.xml 的内容嵌入到当前模型中 -->
  <include file="g1_29dof_rev_1_0.xml"/>

  <!-- 场景的统计信息 -->
  <!-- center 指定场景的中心点坐标（x=1.0, y=0.7, z=1.0） extent 定义场景的范围（以中心点为基准的半径） -->
  <statistic center="1.0 0.7 1.0" extent="0.8"/>

  <visual>
    <!-- 头部光源（跟随相机的光源） -->
    <headlight diffuse="0.6 0.6 0.6" ambient="0.1 0.1 0.1" specular="0.9 0.9 0.9"/>
    <!-- 设置雾（haze）的颜色，模拟大气效果 -->
    <rgba haze="0.15 0.25 0.35 1"/>
    <!-- 设置全局相机视角 -->
    <global azimuth="-140" elevation="-20"/>
  </visual>

  <!-- 仿真场景的资源（纹理和材质），用于渲染地面和天空 -->
  <asset>
    <texture type="skybox" builtin="flat" rgb1="0 0 0" rgb2="0 0 0" width="512" height="3072"/>
    <texture type="2d" name="groundplane" builtin="checker" mark="edge" rgb1="0.2 0.3 0.4" rgb2="0.1 0.2 0.3" markrgb="0.8 0.8 0.8" width="300" height="300"/>
    <material name="groundplane" texture="groundplane" texuniform="true" texrepeat="5 5" reflectance="0.2"/>
  </asset>

  <!-- 场景的物理世界，包括光源和地面几何体 -->
  <worldbody>
    <light pos="1 0 3.5" dir="0 0 -1" directional="true"/>
    <geom name="floor" size="0 0 0.05" type="plane" material="groundplane" friction="1.2 0.02 0.001"/>
  </worldbody>
</mujoco>